This repo contains implementations of core BigFile components, such as a P2P syncing/relay node, database-backed blockchain state manager, single-address wallet, and more. It is intended for use by BigFile node implementations like `walletd`, `renterd`, and `hostd`.
